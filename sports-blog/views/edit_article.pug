extends layout

block content
  .container
    h1 #{title}
    form(method='post', action='/articles/edit/'+article._id)
      .form-group
        label Title:
        input.form-control(name='title', type='text', value=article.title)
      .form-group
        label SubTitle:
        input.form-control(name='subtitle', type='text', value=article.subtitle)
      .form-group
        label Category:
        input.form-control(name='category', type='text', value=article.category)
      .form-group
        label Body:
        textarea.form-control(name='body') #{article.body}
      .form-group
        label Author:
        input.form-control(name='author', type='text', value=article.author)
      input.btn.btn-primary(type='submit', name='Submit', value='Save')
      a.btn.btn-default(href='/manage/articles') Back
      a.btn.btn-danger.delete-category.pull-right(href='/manage/articles', data-cat-id=article._id) Delete
